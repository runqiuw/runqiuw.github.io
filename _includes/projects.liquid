<div class="col">
  <style>
    .project-card-img-container {
      height: 200px;
      overflow: hidden;
      display: flex;
      align-items: center;
      justify-content: center;
      background-color: #f8f9fa;
    }
    .project-card-img-container img {
      width: 100%;
      height: 100%;
      object-fit: cover;
    }
    .project-card-body {
      min-height: 180px;
      display: flex;
      flex-direction: column;
    }
    .project-card-title {
      min-height: 60px;
    }
    .project-card-text {
      flex-grow: 1;
    }
    .project-card-footer {
      margin-top: auto;
    }
  </style>
  <a href="{% if project.redirect %}{{ project.redirect }}{% else %}{{ project.url | relative_url }}{% endif %}">
    <div class="card h-100 hoverable">
      {% if project.img %}
        <div class="project-card-img-container">
          <img src="{{ project.img | relative_url }}" alt="project thumbnail" loading="eager">
        </div>
      {% else %}
        <div class="project-card-img-container"></div>
      {% endif %}
      <div class="card-body project-card-body">
        <h2 class="card-title project-card-title">{{ project.title }}</h2>
        <p class="card-text project-card-text">{{ project.description }}</p>
        <div class="row ml-1 mr-1 p-0 project-card-footer">
          {% if project.github %}
            <div class="github-icon">
              <div class="icon" data-toggle="tooltip" title="Code Repository">
                <a href="{{ project.github }}"><i class="fa-brands fa-github gh-icon"></i></a>
              </div>
              {% if project.github_stars %}
                <span class="stars" data-toggle="tooltip" title="GitHub Stars">
                  <i class="fa-solid fa-star"></i>
                  <span id="{{ project.github_stars }}-stars"></span>
                </span>
              {% endif %}
            </div>
          {% endif %}
        </div>
      </div>
    </div>
  </a>
</div>
